# ──── Backend ────
DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=responsia_tax_dev
NODE_ENV=development
APP_VERSION=0.1.0

# TypeORM SSL (set to true for Azure PostgreSQL)
TYPEORM_MODE_SSL=false

# Auth0 (skipped for v1)
# AUTH0_DOMAIN=your-tenant.eu.auth0.com
# AUTH0_AUDIENCE=https://your-api-identifier

# ──── Frontend ────
VITE_API_URL=http://localhost:3000
# VITE_AUTH0_DOMAIN=your-tenant.eu.auth0.com
# VITE_AUTH0_CLIENT_ID=your-client-id
# VITE_AUTH0_AUDIENCE=https://your-api-identifier

# ──── LLM & OCR (configured via Settings page, not env vars) ────
# These are stored in the database settings table.
# See the Settings page in the UI to configure:
# - Azure OpenAI endpoint, key, API version
# - Azure Anthropic endpoint, key
# - Azure Document Intelligence endpoint, key
# - Default LLM model and system prompt
